{
    "name": "ddns-cloudflare",
    "module": "index.ts",
    "type": "commonjs",
    "scripts": {
        "start": "bun ./index.ts",
        "deploy": "pm2 start bun --name \"ddns-cloudflare\" -- start"
    },
    "devDependencies": {
        "bun-types": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "node-cron": "^3.0.2",
        "node-fetch": "^2.7.0",
        "pm2": "^5.3.0"
    }
}
